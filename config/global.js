const ProductEnum = {
  NEW: "new",
  EXHAUSTED: "exhausted",
  IN_USE: "inuse",
};

const UserEnum = {
  APPROVE: "approve",
  PENDING: "pending",
  DELETE: "delete",
}

const ReplacementStatusEnum = {
  DONE : "done",
  PENDING: "pending",
  CANCELLED : "cancelled",
}

module.exports = { ProductEnum, UserEnum,ReplacementStatusEnum };

